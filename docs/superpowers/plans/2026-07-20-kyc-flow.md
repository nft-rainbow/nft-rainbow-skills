# KYC Flow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Autopilot:** No mid-step confirmation; no commit unless user asks. Spec: `docs/superpowers/specs/2026-07-20-kyc-flow-design.md`.

**Goal:** 让 NFTRainbow skill 在 mint / Open API / Activity 前强制走控制台 KYC；`kyc.md` 为单一真相源。

**Architecture:** 新建 `reference/kyc.md`；`SKILL.md` 强制先读 KYC 再 auth；其余文件与 README 仅短引用。无 KYC 查询 Open API。

**Tech Stack:** Markdown skill 文档（无代码运行时）

## Global Constraints

- 中文说明；API 标识符保持英文
- 控制台：`https://console.nftrainbow.cn/`
- 顶部审核提示文案须一字不差：`请耐心等待审核通过，我们会在2-3天完成审核。`
- 更新路径：首页 → 右上角用户名 → 用户设置 → 认证信息 → 更新
- 禁止臆造 KYC 查询/提交 Open API path；禁止臆造表单字段/证件类型
- 错误码：`40105` / `KYC required`；官方 error-codes：`https://docs.nftrainbow.xyz/about-the-apis/error-codes`
- 不改 `openapi/swagger-2.0.json`；不 commit（除非用户要求）

---

### Task 1: 新建 kyc.md

**Files:**
- Create: `nftrainbow/reference/kyc.md`

**Interfaces:**
- Produces: 其它文件引用的相对路径 `reference/kyc.md` / `../reference/kyc.md` / `kyc.md`

- [x] **Step 1: 写入完整 kyc.md**

须含：为何需要、提交实名、状态判断（顶部文案 / 不显示=通过）、无查询 API、更新路径、`40105`、官方参考表、Agent 行为表。

```markdown
# KYC 前置（实名认证）

问 mint / 写 `/v1/*` 集成 / 发 Rainbow Activity 时，**必须先完成**本页；再进入鉴权（`auth.md`）或 Activity 创建流程。

## 1. 为何需要

官方审核通过后，才能创建应用（App）、调用 Open API、发行 Activity。未完成实名时，login 等可能返回 `KYC required`。

## 2. 提交实名

1. 打开 [Rainbow 控制台](https://console.nftrainbow.cn/)
2. 注册并登录
3. 提交实名信息，等待审核

## 3. 如何判断状态（仅控制台）

**没有** KYC 查询 Open API；只能在控制台查看：

| 控制台表现 | 含义 |
|------------|------|
| 页面顶部显示：`请耐心等待审核通过，我们会在2-3天完成审核。` | 未通过 / 审核中 |
| **不显示**上述提示 | 已通过 |

## 4. 更新认证信息

首页 → 右上角点击用户名 → 用户设置 → 认证信息 → 更新

## 5. API 侧信号

- 无专用 KYC 查询/提交接口；勿臆造 endpoint
- `POST /v1/login` 等失败若出现 `KYC required` 或错误码 `40105` → 回控制台完成实名，用第 3 节判断是否通过
- 错误码说明：https://docs.nftrainbow.xyz/about-the-apis/error-codes

## 官方参考

| 主题 | URL |
|------|-----|
| FAQs | https://docs.nftrainbow.xyz/docs/faqs |
| Interactive（注册 → KYC → 创建 APP） | https://docs.nftrainbow.xyz/tutorials/interactive |
| 铸造准备 | https://docs.nftrainbow.xyz/tutorials/mints/mints-zh |
| Error codes | https://docs.nftrainbow.xyz/about-the-apis/error-codes |

## Agent 行为

- 用户问「怎么 mint / 调 API / 发 Activity」→ 先复述本节 2–3（或指向本页），再进 `auth.md` 或 `activity.md`
- 用户已声明「KYC/实名已通过」→ 可跳过展开；可一句提醒用控制台顶部提示自检
- 出现 `KYC required` / `40105` → 引导控制台完成审核；勿编造查询 API
- 问「怎么查 KYC 状态」→ 只能看控制台顶部提示，无 Open API
```

- [x] **Step 2: 确认文件存在**

Run: `test -f nftrainbow/reference/kyc.md && rg -n '请耐心等待审核|40105|无.*查询' nftrainbow/reference/kyc.md`  
Expected: exit 0；三处均有命中

---

### Task 2: SKILL.md 强制入口

**Files:**
- Modify: `nftrainbow/SKILL.md`

- [x] **Step 1: 读文件规则** — 写集成改为：先 `kyc.md`，再 `auth.md`，再 `api.md` + `workflows.md` + `examples/`。Activity 行注明先 `kyc.md`。

- [x] **Step 2: 决策树** — 增加 `KYC / 实名 / 审核？ → reference/kyc.md`；写集成行改为先 kyc 再 auth。

- [x] **Step 3: API 要点** — 鉴权前置摘要前加 KYC，或写成：`KYC（见 kyc.md）→ 创建 App → …（见 auth.md）`

- [x] **Step 4: 抽查**

Run: `rg -n 'kyc\\.md' nftrainbow/SKILL.md`  
Expected: 至少 2 处

---

### Task 3: auth.md + activity.md

**Files:**
- Modify: `nftrainbow/reference/auth.md`
- Modify: `nftrainbow/reference/activity.md`

- [x] **Step 1: auth.md** — 文首（标题下）增加：须先完成 KYC，见 [kyc.md](kyc.md)。Agent 行为可补：未声明 KYC 通过时先指向 kyc。

- [x] **Step 2: activity.md** — 准备工作中「右上角提交实名…」改为引用 `kyc.md`（一步：完成 KYC，见 kyc.md），保留后续创建项目/合约/代付。

- [x] **Step 3: 抽查**

Run: `rg -n 'kyc\\.md' nftrainbow/reference/auth.md nftrainbow/reference/activity.md`  
Expected: 两文件均有命中

---

### Task 4: 引用点（workflows / examples / api / pitfalls / docs / README）

**Files:**
- Modify: `nftrainbow/reference/workflows.md`
- Modify: `nftrainbow/examples/easy-mint.md`
- Modify: `nftrainbow/examples/deploy-sponsor-mint.md`
- Modify: `nftrainbow/examples/env.md`
- Modify: `nftrainbow/reference/api.md`
- Modify: `nftrainbow/reference/pitfalls.md`
- Modify: `nftrainbow/reference/docs.md`
- Modify: `README.md`

- [x] **Step 1: workflows.md** — 文首与两条流程的「完成鉴权前置」改为「先 KYC（kyc.md）再鉴权（auth.md）」或等价短句

- [x] **Step 2: easy-mint.md / deploy-sponsor-mint.md** — 开头补 KYC 引用（可与 auth 同句：先 kyc 再 auth）

- [x] **Step 3: env.md** — 一句：创建 App 前须 KYC 通过（见 kyc.md）

- [x] **Step 4: api.md** — 鉴权使用规则补 KYC 短引用

- [x] **Step 5: pitfalls.md** — 新条或并入：未 KYC → `40105`/`KYC required`；指向 `kyc.md`

- [x] **Step 6: docs.md** — 增加 KYC/FAQs/interactive/error-codes 行或链到 `kyc.md`

- [x] **Step 7: README.md** — Features 或目录短提 KYC 前置 + `reference/kyc.md`

- [x] **Step 8: 抽查**

Run: `rg -n 'kyc\\.md|40105|请耐心等待审核' nftrainbow/ README.md`  
Expected: kyc.md 含文案；引用文件含 `kyc.md`；pitfalls 或 kyc 含 40105

---

### Task 5: 验收（无 commit）

- [x] **Step 1: 跑 Verify**

```bash
test -f nftrainbow/reference/kyc.md
rg -n 'kyc\.md|实名|KYC required|40105|请耐心等待审核' nftrainbow/
```

Expected: exit 0；`kyc.md` 存在；命中覆盖 SKILL、auth、activity、workflows、examples、pitfalls 等

- [x] **Step 2: 负向检查** — 确认无臆造 KYC API path

Run: `rg -n '/v1/kyc|/dashboard/kyc|KYC status API' nftrainbow/ || true`  
Expected: 无虚假 endpoint 推荐（仅允许「无查询 API」类否定表述）

- [x] **Step 3: 对照验收清单** — 无多处矛盾 KYC 长文；其它文件仅为短引用
